# Claude Rules — XHS ReCreator

See [CLAUDE.md](../../CLAUDE.md) for Claude-specific instructions.
See [AGENTS.md](../../AGENTS.md) for full project context.

## Key Reminders

- `docker compose up -d` (not `restart`) for `.env` changes
- `docker compose build && docker compose up -d` for code changes
- Working directory: `/data/project/xhs_recreator`
- Language: respond in the same language the user uses
